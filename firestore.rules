
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /qr_items/{uuid} {
      allow read: if false;
      allow write: if false;
    }
    match /qr_private/{uuid} {
      allow read, write: if false;
    }
  }
}
